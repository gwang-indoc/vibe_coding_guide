# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`vibe_coding_guide` — a new, empty repository. No build system, test runner, or source files exist yet.

## IDE

JetBrains IDE configured with Kubernetes API provider (`.idea/misc.xml`). Add `.idea/` to `.gitignore` if workspace files should not be tracked.